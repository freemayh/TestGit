idle에 잘못 입력했을 경우 : 잘못 입력된 줄 클릭후 - [enter] 누르면 >>> 뒤에 입력됨
네 칸 띄어쓰기 : 추천
거북이 : 꼬리에 잉크 
down()/pendown() : 꼬리 내림 - 선 그려짐
up()/penup() : 꼬리 올림 - 선 그려지지 않음

함수 에서 사용되는 값 정의 하는 인자 : 형식인자, 매개변수 
호출시 실제 값 : 실 인자, 인자, 인수

t.mainloop() # 사용자가 그래픽 창을 종료할 때까지 프로그램을 실행하면서 마우스나 키보드 입력을 계속 처리하도록 하는 함수 
// t.done()
https://docs.python.org/3.4/library/turtle.html#turtle.done

eval("4+5") # evaluation 식만 계산,  문자열을 수식으로 처리해서 계산 후 결괏값 반환
exec // 문자열로 된 문(Statement) 수행
# 여러줄
exec("""
def prt():
	print('hello world')
""")

print(*objects, sep=' ', end='\n', file=sys.stdout, flush=False)

fruit=["apple", "grape", "persimmon", "strawberry"]
print( random.choice(fruit) ) // 리스트에서 하나 추출
#Return a random element from the non-empty sequence seq. If seq is empty, raises IndexError.
format(et, ".2f")  # 보기 좋게 소수점 2자리까지만 표기합니다.

import module
from module import foo

# 1~5 사이에서 뽑은 수가 3이면(20% 확률)
if random.randint(1, 5) == 3:      

import sys
sys.exit()    # 프로그램 실행을 멈춥니다.

*일시 중지
import time
time.sleep(5)

*달력 
import calendar
calndar.prmonth(2016, 10) # 그 달의 달력 출력
calendar.prcal(2016) #연도 